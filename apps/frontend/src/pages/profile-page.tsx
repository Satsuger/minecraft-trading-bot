import { PlaceholderView } from "../components/placeholder-view.js";

export function ProfilePage() {
  return (
    <PlaceholderView
      body="This section is reserved for bot identity, session details, and account-specific settings."
      eyebrow="Profile"
      title="Profile Workspace"
    />
  );
}

import { PlaceholderView } from "../components/placeholder-view.js";

export function OrdersPage() {
  return (
    <PlaceholderView
      body="This section is reserved for tracked orders, order status, and order actions."
      eyebrow="Orders"
      title="Orders Workspace"
    />
  );
}
